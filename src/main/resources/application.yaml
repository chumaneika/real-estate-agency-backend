server:
  port: 8080

spring:
  application:
    name: real-estate-agency

  datasource:
    url: "jdbc:mysql://localhost:3306/term_paper?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true"
    username: term_paper_app
    password: term_paper_local_2026
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: ${DDL_AUTO:update}
    show-sql: ${SHOW_SQL:true}
    properties:
      hibernate:
        format_sql: ${FORMAT_SQL:true}
